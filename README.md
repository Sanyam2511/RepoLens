# RepoLens – The Visual Architecture Mapper
A tool to visualize public/private GitHub repositories in an interactive 3D map.

## Project Structure
- `apps/web`: Next.js Frontend (WebGL/React Flow)
- `apps/worker`: Node.js/TypeScript AST Analysis Engine
- `packages/shared`: Shared Types & Interfaces